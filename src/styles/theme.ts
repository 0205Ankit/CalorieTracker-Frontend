import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#686f78",
      light: "#f6f8fa",
      dark: "#22262f",
    },
  },
});
export default theme